{
  "name": "sauce-demo-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "npx cypress run"
  },
  "devDependencies": {
    "cypress": "^14.0.0"
  }
}
